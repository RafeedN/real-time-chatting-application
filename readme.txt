Name: Rafeed Nazmul
Modules: socket.io
Launch:  node .\server.js 
URL: http://localhost:3000/connect.html
NodeJS Version: v18.14.0
Note: To get multiple servers connected you have to go to http://localhost:3000/connect.html each time and then add a username then connect
Note: After conncting to a server do not refresh the page or all the usernames will be the same. So for example, after connecting one server with "Mike" and another with "Don" do not refresh either page or both will be the same name;
